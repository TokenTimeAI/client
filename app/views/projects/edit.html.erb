<% content_for :title, "Edit #{@project.name} — ttime.ai" %>

<div class="max-w-xl">
  <h1 class="text-2xl font-bold text-white mb-6">Edit Project</h1>

  <%= render "form", project: @project %>
</div>
